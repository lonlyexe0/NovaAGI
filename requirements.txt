# ═══════════════════════════════════════════════════════════════════════════════
# requirements.txt — Nova AGI (Linux) çekirdek bağımlılıkları
# ═══════════════════════════════════════════════════════════════════════════════
# PyTorch'u GPU'nuza uygun derlemeden kurmak için ./install.sh kullanın
# (NVIDIA CUDA / AMD ROCm / Intel XPU / CPU otomatik seçilir). Elle kurulum:
#   pip install torch --index-url https://download.pytorch.org/whl/rocm6.2   (AMD)
#   pip install torch --index-url https://download.pytorch.org/whl/cpu       (CPU)
#   pip install torch                                                         (NVIDIA)
torch>=2.2

numpy>=1.24
requests>=2.28
beautifulsoup4>=4.12
psutil>=5.9
pillow>=10.0
mss>=9.0              # hızlı X11 ekran görüntüsü
edge-tts>=6.1         # nöral sesli yanıt (internet gerekir; çevrimdışı yedek: espeak-ng)
