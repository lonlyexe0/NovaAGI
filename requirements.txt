# ═══════════════════════════════════════════════════════════════════════════════
# requirements.txt  —  Nova AGI Bağımlılıkları
# ═══════════════════════════════════════════════════════════════════════════════

# ── 1. Derin Öğrenme & GPU Hızlandırma ──────────────────────────────────────────
torch>=2.0.0
torchvision
torchaudio
# Windows AMD/Intel/NVIDIA GPU Desteği (DirectML)
torch-directml>=0.2.0; platform_system == "Windows"

# ── 2. Semantik & Vektörel Hafıza (RAG) ────────────────────────────────────────
sentence-transformers>=2.2.0
numpy>=1.24.0
scipy>=1.10.0

# ── 3. HuggingFace & Sürekli Veri Akışı ────────────────────────────────────────
datasets>=2.14.0
huggingface-hub>=0.16.0
pyarrow>=12.0.0

# ── 4. Web Tarama & Bilgi Toplama ──────────────────────────────────────────────
requests>=2.28.0
beautifulsoup4>=4.12.0

# ── 5. Ajan Beden: Bilgisayar & Arayüz Kontrolü ─────────────────────────────────
pillow>=9.0.0
pyautogui>=0.9.50
pyperclip>=1.8.0

# ── 6. İsteğe Bağlı: Görüntü & Ses Modülleri (Opsiyonel) ───────────────────────
# Kamera ve OCR:
opencv-python>=4.7.0
pytesseract>=0.3.10

# Ses Girişi / Çıkışı (STT & TTS):
SpeechRecognition>=3.10.0
pyttsx3>=2.90
# PyAudio (Windows için gerekirse: pip install pyaudio)
# pyaudio>=0.2.13

# ── Standart Python Kütüphaneleri (Harici Kurulum Gerekmez) ────────────────────
# sqlite3, tkinter, threading, queue, importlib, subprocess, json, re, math, os, signal
