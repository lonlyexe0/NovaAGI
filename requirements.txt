# ═══════════════════════════════════════════════════════════════════════════════
# requirements.txt  —  Nova AGI Bağımlılıkları
# ═══════════════════════════════════════════════════════════════════════════════

# ── Temel Çekirdek Bağımlılıkları (Core) ──────────────────────────────────────
# Derin Öğrenme (PyTorch — CPU / ROCm / CUDA / DirectML otomatik seçilir)
torch>=2.0.0

# Web Taraması ve Bilgi Toplama
requests>=2.28.0
beautifulsoup4>=4.12.0

# HuggingFace Sürekli Canlı Veri Akışı (Wikipedia / Dataset Akışı)
datasets>=2.14.0

# ── Opsiyonel Gelişmiş Beden Yetenekleri (Body / Vision / Audio) ──────────────
# Ekran Görüntüsü ve Görüntü İşleme
Pillow>=10.0.0
opencv-python>=4.8.0

# Bilgisayar & Fare & Klavye Otomasyonu (body.py)
pyautogui>=0.9.54

# Ses Motoru (Konuşma & Dinleme)
SpeechRecognition>=3.10.0
pyttsx3>=2.90
edge-tts>=6.1.0
# Linux mikrofonu için PyAudio derleme gereksinimi dağıtıma göre değişebilir.
# Debian/Ubuntu: sudo apt install portaudio19-dev

# ── Standart Python Kütüphaneleri (Ekstra kurulum gerektirmez) ─────────────────
# sqlite3, threading, tkinter, queue, importlib, subprocess, json, re, math, os
